# Build output
/tradectl
*.test
*.out
*.exe

# Go workspace / vendor
/vendor/
go.work
go.work.sum

# Local environment overrides. The committed defaults are throwaway container
# credentials; anything real belongs here and never in the repo.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local data. Postgres, Redpanda, and ClickHouse volumes are managed by
# docker compose, not by the repo.
/data/
